[package]
name = "rvoip-users-core"
version.workspace = true
edition.workspace = true
license.workspace = true
homepage.workspace = true
repository.workspace = true
documentation.workspace = true
authors.workspace = true
rust-version.workspace = true
categories.workspace = true
keywords.workspace = true
description = "User management and authentication service for RVoIP"

# Published on crates.io as `rvoip-users-core` (rvoip- prefix convention), but
# the lib/import name stays `users_core` so existing `use users_core::...` in
# tests, examples, and consumers keeps working — same split as rvoip-codec-core
# (package `rvoip-codec-core`, lib `codec_core`).
[lib]
name = "users_core"

[features]
default = []
auth-core = ["dep:rvoip-auth-core", "dep:rvoip-core-traits"]
client = ["reqwest"]
postgres = ["dep:sqlx-postgres"]

[dependencies]
# Async runtime
tokio = { workspace = true, features = ["full"] }

# Database
sqlx-core = { version = "0.8", default-features = false, features = ["_rt-tokio", "json"] }
sqlx-sqlite = { version = "0.8", default-features = false, features = ["bundled", "chrono", "json", "uuid"] }
sqlx-postgres = { version = "0.8", default-features = false, features = ["chrono", "json", "uuid"], optional = true }

# Password hashing
argon2 = "0.5"
password-hash = "0.5"

# JWT
jsonwebtoken = { version = "10.4", features = ["rust_crypto"] }
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }

# UUID generation
uuid = { version = "1.11", features = ["v4", "serde"] }

# Date/time
chrono = { version = "0.4", features = ["serde"] }

# Error handling
anyhow = { workspace = true }
thiserror = { workspace = true }

# Async traits
async-trait = "0.1"

# Web framework (for REST API)
axum = { version = "0.7", features = ["http2", "json", "tokio", "macros"] }
axum-server = { version = "0.8", default-features = false, features = ["tls-rustls-no-provider"] }
tower-http = { version = "0.6", features = ["cors", "trace"] }

# Logging
tracing = { workspace = true }
tracing-subscriber = { workspace = true, features = ["env-filter", "json"] }

# Configuration
config = "0.14"

# Cryptography
rand = "0.8"
base64 = "0.22"
sha2 = "0.10"
hex = "0.4"
md5 = "0.7"
rsa = { version = "0.9", features = ["pem"] }

# HTTP client (for examples)
reqwest = { version = "0.12", features = ["json"], optional = true }

# Validation
validator = { version = "0.20", features = ["derive"] }
regex = "1.10"
once_cell = "1.19"
ipnet = "2.12"
rvoip-auth-core = { workspace = true, optional = true }
rvoip-core-traits = { workspace = true, optional = true }

[dev-dependencies]
tempfile = "3.14"
reqwest = { version = "0.12", features = ["json"] }
serde_json = "1.0"
futures = "0.3"
tower = { version = "0.5", features = ["util"] }

[[example]]
name = "users_core_basic_usage"
path = "examples/basic_usage.rs"

[[example]]
name = "sip_register_flow"
path = "examples/sip_register_flow.rs"

[[example]]
name = "token_validation"
path = "examples/token_validation.rs"

[[example]]
name = "api_key_service"
path = "examples/api_key_service.rs"

[[example]]
name = "multi_device_presence"
path = "examples/multi_device_presence.rs"

[[example]]
name = "session_core_v2_integration"
path = "examples/session_core_v2_integration.rs"

[[example]]
name = "rest_api_demo_server"
path = "examples/rest_api_demo/server.rs"

[[example]]
name = "rest_api_demo_client"
path = "examples/rest_api_demo/client.rs"
required-features = ["client"]

[[test]]
name = "security_sql_injection_tests"
path = "tests/security_sql_injection_tests.rs"

[[test]]
name = "security_timing_attack_tests"
path = "tests/security_timing_attack_tests.rs"

[[test]]
name = "security_input_validation_tests"
path = "tests/security_input_validation_tests.rs"

[[test]]
name = "security_rate_limiting_tests"
path = "tests/security_rate_limiting_tests.rs"

[[test]]
name = "auth_core_bridge_tests"
path = "tests/auth_core_bridge_tests.rs"
required-features = ["auth-core"]

[lints]
workspace = true
