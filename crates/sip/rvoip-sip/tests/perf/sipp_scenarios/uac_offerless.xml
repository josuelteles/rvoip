<?xml version="1.0" encoding="ISO-8859-1" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">

<!--
  Delayed-offer (offerless) UAC — RFC 3261 §14.2 / RFC 6337 §3.2.

  Drives the exchange that regressed in the upstream merge and was fixed in
  cdecbd8c: the caller sends an INVITE with no SDP, the callee puts its offer
  in the 200 OK, and the caller's answer travels in the ACK.

  This exists because the only coverage of that path was rvoip talking to
  rvoip, where both ends can share the same misreading. sipp is an
  independent implementation of the wire contract.

  The scenario fails, rather than merely reporting, when the 200 OK does not
  carry an SDP offer: `check_it="true"` on both ereg actions turns a missing
  Content-Type or a bodyless 200 into a failed call.
-->

<scenario name="rvoip_offerless_uac">

  <!-- INVITE with no offer. Content-Length: 0 and no body at all. -->
  <send retrans="500">
    <![CDATA[

      INVITE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: sipp <sip:sipp@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: sut <sip:[service]@[remote_ip]:[remote_port]>
      Call-ID: [call_id]
      CSeq: 1 INVITE
      Contact: <sip:sipp@[local_ip]:[local_port];transport=[transport]>
      Max-Forwards: 70
      Subject: Offerless INVITE
      Content-Length: 0

    ]]>
  </send>

  <recv response="100" optional="true" />
  <recv response="180" optional="true" />

  <!-- The 200 OK must carry the callee's offer. Both checks are assertions. -->
  <recv response="200" rtd="true">
    <action>
      <ereg regexp="application/sdp"
            search_in="hdr" header="Content-Type:"
            check_it="true" assign_to="sdp_ctype" />
      <ereg regexp="^v=0"
            search_in="body"
            check_it="true" assign_to="sdp_body" />
      <!-- sipp refuses to load a scenario whose assigned variables are never
           read, and `check_it` has no effect without `assign_to`. Logging
           them is what makes both assertions loadable. -->
      <log message="delayed offer: Content-Type=[$sdp_ctype] body=[$sdp_body]" />
    </action>
  </recv>

  <!-- The answer travels in the ACK. -->
  <send>
    <![CDATA[

      ACK sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: sipp <sip:sipp@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: sut <sip:[service]@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 1 ACK
      Contact: <sip:sipp@[local_ip]:[local_port];transport=[transport]>
      Max-Forwards: 70
      Subject: Offerless answer
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=sipp 53655765 2353687637 IN IP[local_ip_type] [local_ip]
      s=-
      c=IN IP[media_ip_type] [media_ip]
      t=0 0
      m=audio [media_port] RTP/AVP 0
      a=rtpmap:0 PCMU/8000
      a=sendrecv

    ]]>
  </send>

  <!-- Hold the call long enough that a teardown like the one in the
       regression (BYE ~250 ms after both sides reach Active) lands inside
       the scenario and is seen as an unexpected message. -->
  <pause milliseconds="2000" />

  <send retrans="500">
    <![CDATA[

      BYE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: sipp <sip:sipp@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: sut <sip:[service]@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 2 BYE
      Contact: <sip:sipp@[local_ip]:[local_port];transport=[transport]>
      Max-Forwards: 70
      Subject: Offerless teardown
      Content-Length: 0

    ]]>
  </send>

  <recv response="200" crlf="true" />

  <ResponseTimeRepartition value="10, 20, 50, 100, 200, 500, 1000" />
</scenario>
